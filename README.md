# SubRMatch API

**SubRMatch** - это FastAPI приложение для анализа и форматирования постов Reddit с использованием искусственного интеллекта. Сервис помогает пользователям находить подходящие сабреддиты для своих постов и форматировать их согласно правилам выбранного сообщества.

## 📋 Оглавление

- [Возможности](#возможности)
- [Технологический стек](#технологический-стек)
- [Требования](#требования)
- [Установка](#установка)
- [Настройка](#настройка)
- [Запуск проекта](#запуск-проекта)
- [Миграции базы данных](#миграции-базы-данных)
- [API Документация](#api-документация)
- [Структура проекта](#структура-проекта)
- [Основные компоненты](#основные-компоненты)
- [Разработка](#разработка)

## ✨ Возможности

### Анализ Reddit постов
- **Предложение сабреддитов**: AI анализирует ваш пост и предлагает подходящие сабреддиты для публикации
- **Форматирование постов**: Автоматическое форматирование поста согласно правилам выбранного сабреддита
- **Получение правил**: Интеграция с Reddit API для автоматического получения правил сообществ

### Система аутентификации
- **Регистрация через email**: Классическая регистрация с подтверждением email
- **Google OAuth 2.0**: Вход через Google аккаунт
- **JWT токены**: Безопасная аутентификация с access и refresh токенами
- **Восстановление пароля**: Механизм сброса пароля через email
- **Верификация email**: Подтверждение email адреса перед использованием аккаунта

### Дополнительные возможности
- **Rate Limiting**: Ограничение количества запросов для защиты API
- **Асинхронная обработка**: Все операции выполняются асинхронно
- **Фоновые задачи**: Отправка email через Celery
- **Streaming ответы**: Постепенная отправка ответов от AI моделей

## 🛠 Технологический стек

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **SQLModel** - ORM на базе SQLAlchemy и Pydantic
- **Alembic** - система миграций базы данных
- **Pydantic** - валидация данных

### База данных
- **PostgreSQL** - основная реляционная база данных
- **Redis** - кэширование и хранилище для JWT токенов

### Очереди задач
- **Celery** - распределенная система очередей задач
- **Redis** - брокер сообщений для Celery

### AI/ML
- **OpenAI API** / **OpenRouter** - интеграция с AI моделями (DeepSeek Chat)
- Поддержка streaming ответов

### Аутентификация
- **PyJWT** - работа с JWT токенами
- **Authlib** - OAuth 2.0 клиент для Google
- **bcrypt** - хеширование паролей

### Дополнительно
- **aiohttp** - асинхронные HTTP запросы к Reddit API
- **fastapi-mail** - отправка email
- **slowapi** - rate limiting
- **uvicorn** - ASGI сервер

## 📦 Требования

- **Python** 3.9+
- **PostgreSQL** 12+
- **Redis** 6+
- **Celery** worker (опционально, для фоновых задач)

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd Base-FastApi-Project
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Активация виртуального окружения
# Linux/macOS:
source venv/bin/activate

# Windows:
venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements/prod.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта со следующими переменными:

```env
# AI Configuration
AI_API_KEY=your_openrouter_or_openai_api_key
AI_API_URL=https://openrouter.ai/api/v1/chat/completions

# Reddit API Configuration
REDDIT_CLIENT_ID=your_reddit_client_id
REDDIT_CLIENT_SECRET=your_reddit_client_secret
REDDIT_USER_AGENT=your_app_name/1.0 by your_username
REDDIT_BASE_URL=https://oauth.reddit.com
REDDIT_USER_NAME=your_reddit_username
REDDIT_USER_PASSWORD=your_reddit_password

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# JWT Configuration
JWT_SECRET_KEY=your_secret_key_here_min_32_chars
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10
REFRESH_TOKEN_EXPIRE_DAYS=7

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password
MAIL_FROM_NAME=SubRMatch
MAIL_VERIFICATION_COOLDOWN=300

# Verification Token
VERIFICATION_TOKEN_SECRET=your_verification_secret_key
VERIFICATION_TOKEN_LIFETIME_HOURS=24

# Application Configuration
APP_URL=http://localhost:8000
APP_NAME=SubRMatch
DOMAIN=localhost:8000
SECRET_KEY=your_secret_key_for_sessions

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=subrmatch
DB_USER=postgres
DB_PASSWORD=your_db_password

# Redis Configuration
REDIS_URL=redis://localhost:6379/
REDIS_HOST=localhost
REDIS_PORT=6379
```

### 5. Настройка базы данных

Создайте базу данных PostgreSQL:

```sql
CREATE DATABASE subrmatch;
```

### 6. Запуск миграций

```bash
alembic upgrade head
```

## ⚙️ Настройка

### Получение Reddit API ключей

1. Перейдите на https://www.reddit.com/prefs/apps
2. Создайте новое приложение (script type)
3. Скопируйте Client ID и Client Secret

### Настройка Google OAuth

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google+ API
4. Создайте OAuth 2.0 Client ID
5. Добавьте authorized redirect URI: `http://localhost:8000/api/1.0/auth/google/callback`
6. Скопируйте Client ID и Client Secret

### Настройка Email (Gmail)

1. Включите двухфакторную аутентификацию
2. Создайте "Пароль приложения" в настройках аккаунта Google
3. Используйте этот пароль в `SMTP_PASSWORD`

## 🏃 Запуск проекта

### Запуск основного приложения

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Приложение будет доступно по адресу: `http://localhost:8000`

### Запуск Celery Worker (для фоновых задач)

В отдельном терминале:

```bash
celery -A services.celery.celery_tasks.c_app worker --loglevel=info
```

### Запуск Flower (мониторинг Celery)

```bash
celery -A services.celery.celery_tasks.c_app flower
```

Flower будет доступен по адресу: `http://localhost:5555`

## 📊 Миграции базы данных

### Создание новой миграции

```bash
alembic revision --autogenerate -m "описание изменений"
```

### Применение миграций

```bash
alembic upgrade head
```

### Откат миграции

```bash
alembic downgrade -1
```

## 📚 API Документация

После запуска приложения доступна интерактивная документация:

- **Swagger UI**: `http://localhost:8000/docs`
- **ReDoc**: `http://localhost:8000/redoc`

### Основные эндпоинты

#### Аутентификация (`/api/1.0/auth`)

- `POST /api/1.0/auth/signup` - Регистрация нового пользователя
- `POST /api/1.0/auth/login` - Вход в систему
- `POST /api/1.0/auth/refresh_token` - Обновление access токена
- `POST /api/1.0/auth/logout` - Выход из системы
- `POST /api/1.0/auth/me` - Получение информации о текущем пользователе
- `POST /api/1.0/auth/resend_verification` - Повторная отправка письма верификации
- `POST /api/1.0/auth/verify/{token}` - Подтверждение email адреса
- `POST /api/1.0/auth/password_reset` - Запрос на сброс пароля
- `POST /api/1.0/auth/password_reset_confirm/{token}` - Подтверждение сброса пароля
- `GET /api/1.0/auth/google/login` - Авторизация через Google
- `GET /api/1.0/auth/google/callback` - Callback для Google OAuth
- `GET /api/1.0/auth/oauth/tokens` - Получение токенов после OAuth

#### Анализ Reddit (`/api/1.0/reddit_analyzer`)

- `POST /api/1.0/reddit_analyzer/suggest_subreddits` - Получение предложений подходящих сабреддитов (streaming)
- `POST /api/1.0/reddit_analyzer/format_post` - Форматирование поста для конкретного субреддита (streaming)

### Примеры запросов

#### Регистрация

```bash
curl -X POST "http://localhost:8000/api/1.0/auth/signup" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "secure_password",
    "name": "John Doe",
    "redirect_uri": "http://localhost:3000/verify/"
  }'
```

#### Вход

```bash
curl -X POST "http://localhost:8000/api/1.0/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "secure_password"
  }'
```

#### Предложение сабреддитов

```bash
curl -X POST "http://localhost:8000/api/1.0/reddit_analyzer/suggest_subreddits" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "post": "Какой-то текст вашего поста..."
  }'
```

## 📁 Структура проекта

```
Base-FastApi-Project/
├── api/                        # API эндпоинты
│   ├── endpoints/
│   │   ├── auth.py            # Эндпоинты аутентификации
│   │   └── reddit_analyzer.py # Эндпоинты анализа Reddit
│   └── utils.py               # Утилиты API (rate limiting)
├── core/
│   └── config.py              # Конфигурация приложения
├── database/
│   ├── db.py                  # Настройка базы данных
│   └── redis.py               # Работа с Redis
├── middleware/
│   ├── logging_middleware.py  # Middleware для логирования
│   └── main_middleware.py     # Основные middleware
├── migrations/                # Миграции Alembic
│   └── versions/
├── models/
│   ├── db/                    # SQLModel модели
│   │   └── users.py
│   └── pydantic/              # Pydantic модели
│       ├── auth.py
│       ├── reddit.py
│       └── validators/        # Валидаторы
├── services/
│   ├── ai/                    # AI сервисы
│   │   ├── ollama/
│   │   ├── prompts.py
│   │   └── utils.py
│   ├── auth/                  # Сервисы аутентификации
│   │   ├── dependencies.py
│   │   ├── user_service.py
│   │   └── utils/
│   ├── celery/                # Celery задачи
│   │   └── celery_tasks.py
│   ├── email/                 # Сервис отправки email
│   │   └── mail.py
│   ├── errors/                # Обработка ошибок
│   ├── oauth/                 # OAuth интеграции
│   │   └── google_oauth.py
│   └── reddit/                # Reddit API клиент
│       ├── service.py
│       └── utils.py
├── templates/
│   └── emails/                # HTML шаблоны для email
├── tests/                     # Тесты
├── requirements/
│   └── prod.txt               # Зависимости проекта
├── alembic.ini                # Конфигурация Alembic
├── main.py                    # Точка входа приложения
└── README.md                  # Документация
```

## 🔧 Основные компоненты

### Система аутентификации

- **JWT токены**: Access токены (короткоживущие) и Refresh токены (долгоживущие)
- **Хранилище токенов**: Токены хранятся в Redis для возможности отзыва
- **Роли пользователей**: Поддержка ролей (user, admin)
- **Верификация email**: Обязательная верификация для email регистрации
- **Google OAuth**: Бесшовная интеграция с Google аккаунтами

### AI интеграция

- **OpenRouter API**: Использование различных AI моделей через OpenRouter
- **DeepSeek Chat**: Модель по умолчанию для анализа постов
- **Streaming ответы**: Постепенная отправка ответов для лучшего UX
- **Обработка правил**: Автоматическое извлечение и анализ правил сабреддитов

### Reddit API

- **OAuth 2.0**: Авторизация через Reddit API
- **Получение правил**: Автоматическое извлечение правил сабреддитов
- **Проверка существования**: Валидация существования сабреддита перед обработкой

### Фоновые задачи

- **Celery**: Асинхронная обработка email отправки
- **Redis как брокер**: Использование Redis для очереди задач
- **Шаблоны email**: HTML шаблоны для верификации и сброса пароля

## 🧪 Разработка

### Запуск в режиме разработки

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Флаг `--reload` автоматически перезагружает сервер при изменениях в коде.

### Линтинг и форматирование

Рекомендуется использовать:
- **black** - форматирование кода
- **flake8** или **pylint** - проверка стиля кода
- **mypy** - проверка типов

### Тестирование

Тесты находятся в директории `tests/`. Для запуска:

```bash
pytest
```

### Rate Limiting

Приложение использует `slowapi` для ограничения количества запросов. Лимиты настроены на уровне эндпоинтов:

- Аутентификация: 2-4 запроса в секунду
- Анализ Reddit: 1 запрос в секунду

### Middleware

- **CORS**: Настроен для работы с фронтенд приложениями
- **Логирование**: Автоматическое логирование запросов
- **Обработка ошибок**: Централизованная обработка исключений

## 🔒 Безопасность

- **Хеширование паролей**: Использование bcrypt для безопасного хранения паролей
- **JWT токены**: Безопасная передача аутентификационных данных
- **Rate Limiting**: Защита от DDoS атак и злоупотреблений
- **Валидация данных**: Строгая валидация всех входящих данных через Pydantic
- **Секретные ключи**: Хранение чувствительных данных в переменных окружения
- **HTTPS**: Рекомендуется использовать HTTPS в production

**Примечание**: Убедитесь, что все переменные окружения правильно настроены перед запуском приложения. Особенно важно проверить настройки базы данных, Redis и API ключи.

